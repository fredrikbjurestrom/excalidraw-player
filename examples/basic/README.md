# Basic example

A neutral, synthetic two-scene example for Excalidraw Player. It contains no application or customer data.

Run from the workspace root:

```bash
npm run dev:basic
```

The first scene shows a direct request. The second introduces a gateway and deliberately contains two arrows with the same label to demonstrate disambiguation through `from` and `to` selectors.
